/**
 * Vertical space reserved above the tallest bar for the total label and the
 * add button, so a full-height bar never pushes them out of the plot.
 */
export const HEADROOM = 36;

/** Height of the bar caption strip below the plot. */
export const CAPTION_HEIGHT = 34;

/** Minimum element height in px before its label is worth drawing. */
export const LABEL_MIN_HEIGHT = 15;

/** Minimum height the plot is allowed to collapse to. */
export const MIN_PLOT_HEIGHT = 80;
